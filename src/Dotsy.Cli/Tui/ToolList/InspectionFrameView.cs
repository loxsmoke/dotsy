namespace Dotsy.Cli.Tui.ToolList;

// Full-width overlay frame for tool-output inspection and file diffs. Its content view is a
// ScrollableText, which draws its own vertical and horizontal scroll bars, so this frame is just
// a titled border around it.
internal sealed class InspectionFrameView : FrameView
{
}
